import React from "react";
import './header.css'


// eslint-disable-next-line import/no-anonymous-default-export
export default () => {
    return (
        <header>
            ola
        </header>
    );
}